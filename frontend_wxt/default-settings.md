# Default Settings (Fresh Install)

Initial Redux values applied on first install (requested defaults).

## Global
- Power: `true` (enabled)
- Theme: `scrollr`
- Layout: `mode=comfort`, `speed=classic`, `position=bottom`, `opacity=1.0`

## Toggles
- Debug: `false`, categories: `[]`
- Sports toggles: `NFL=false`, `NBA=false`, `MLB=false`, `NHL=false`
- Fantasy: `YAHOO_FANTASY=false`

## Finance
- Stocks: `enabled=true`, `activePreset=null`, all stock symbols default `true`, `searchTerm=''`
- Crypto: `enabled=false`, `activePreset=null`, `searchTerm=''`

## RSS
- Enabled: `true`
- Default feeds: NPR Top Stories (others available but disabled)
- Selections: only NPR enabled by default
- Search term: `''`

## Fantasy
- Enabled: `false`
- Sort: `sortKey=''`, `sortDir='desc'`
- Date: `dateMode='today'`, `date=''`
- Filters: `typeFilter='all'`, `showExtras=true`

## Pinned Items
- Empty list by default
