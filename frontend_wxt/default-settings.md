# Default Settings (Fresh Install)

Initial Redux values before any user changes or persisted state are applied.

## Global
- Power: `true` (enabled)
- Theme: `scrollr`
- Layout: `mode=compact`, `speed=classic`, `position=bottom`, `opacity=1.0`

## Toggles
- Debug: `false`, categories: `[]`
- Sports toggles: `NFL=false`, `NBA=false`, `MLB=false`, `NHL=false`
- Fantasy: `YAHOO_FANTASY=true`

## Finance
- Stocks: `enabled=false`, `activePreset=null`, all stock symbols default `false`, `searchTerm=''`
- Crypto: `enabled=true`, `activePreset='majors'`, majors set per `CRYPTO_OPTIONS` defaults, `searchTerm=''`

## RSS
- Enabled: `true`
- Default feeds: NPR Top Stories, CBS News Latest, PBS NewsHour Economy, Financial Times Front Page, CNN Top Stories, New York Times Front Page
- Selections: all defaults `true`
- Search term: `''`

## Fantasy
- Enabled: `true`
- Sort: `sortKey=''`, `sortDir='desc'`
- Date: `dateMode='today'`, `date=''`
- Filters: `typeFilter='all'`, `showExtras=true`

## Pinned Items
- Empty list by default
